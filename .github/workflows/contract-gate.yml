name: contract-gate

on:
  push:
    branches: [main]
  pull_request:

jobs:
  contract-linux:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y python3 unzip curl xvfb libx11-6 libgl1

      - name: Setup Node.js
        uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af # v4.1.0
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: mcp/package-lock.json

      - name: Install and verify Godot
        run: |
          chmod +x scripts/godot-runtime
          echo "ICONFORGE_GODOT=$(./scripts/godot-runtime install)" >> "$GITHUB_ENV"
          ./scripts/godot-runtime bootstrap

      - name: Run contract gate
        run: ./scripts/contract-gate

      - name: Upload CI artifacts on failure
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: contract-gate-linux
          path: out/ci-artifacts/contract
          if-no-files-found: warn

  contract-windows:
    runs-on: windows-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup Node.js
        uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af # v4.1.0
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: mcp/package-lock.json

      - name: Install Godot
        shell: bash
        run: |
          chmod +x scripts/godot-runtime
          echo "ICONFORGE_GODOT=$(./scripts/godot-runtime install)" >> "$GITHUB_ENV"
          ./scripts/godot-runtime bootstrap

      - name: Run contract gate
        shell: bash
        run: ./scripts/contract-gate

  contract-macos:
    runs-on: macos-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup Node.js
        uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af # v4.1.0
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: mcp/package-lock.json

      - name: Install Godot
        shell: bash
        run: |
          chmod +x scripts/godot-runtime
          echo "ICONFORGE_GODOT=$(./scripts/godot-runtime install)" >> "$GITHUB_ENV"
          ./scripts/godot-runtime bootstrap

      - name: Run contract gate
        shell: bash
        run: ./scripts/contract-gate
